import { dayTemplate } from "./dayTemplate"

describe('Day Template', () => {
    describe('Part 1', () => {
        test('example', () => {
            // when
            const result = dayTemplate()

            // then
            expect(result).toBe('')
        })

        test('real challenge', () => {
            // when
            const result = dayTemplate()

            // then
            expect(result).toBe('')
        })
    })

    describe('Part 2', () => {
        test('example', () => {
            // when
            const result = dayTemplate()

            // then
            expect(result).toBe('')
        })

        test('real challenge', () => {
            // when
            const result = dayTemplate()

            // then
            expect(result).toBe('')

        })
    })
})